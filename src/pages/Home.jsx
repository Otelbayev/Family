import React from "react";
import { NavLink } from "react-router-dom";

const Home = () => {
  return (
    <div>
      <NavLink to="family-admin">Login</NavLink>
    </div>
  );
};

export default Home;
